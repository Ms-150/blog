# 文件上传

```html
<input type="file" />
```

## 单个上传

input `accept` 属性 限制文件类型

```html
<input type="file" accept=“images/*”>
```

## 多文件

input `multiple` 属性 限制文件类型

```html
<input type="file" accept=“images/*” multiple>
```

Spark

## BackEnd 后台

Node. Express

Multiparty

TODO
